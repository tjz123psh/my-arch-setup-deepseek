# Maintainer: Christopher Cooper <christopher@cg505.com>
# Maintainer: Jérôme Poulin <jeromepoulin@gmail.com>
# Maintainer: Fabio Fontana (fabifont) <me@fabifont.dev>

pkgname=claude-code
pkgver=2.1.220
pkgrel=1
pkgdesc='An agentic coding tool that lives in your terminal'
arch=('x86_64')
url='https://github.com/anthropics/claude-code'
license=('LicenseRef-claude-code')
depends=('bash')
options=('!strip')
optdepends=(
  'git: allow Claude to use git'
  'github-cli: interact with GitHub'
  'glab: interact with GitLab'
  'ripgrep: enhanced file search'
  'tmux: agent team split panes'
  'bubblewrap: sandboxing'
  'socat: sandboxing'
)

source=('cc-legal')
source_x86_64=("claude-${pkgver}-x86_64::https://downloads.claude.ai/claude-code-releases/${pkgver}/linux-x64/claude")
sha256sums=('d8814ea0d3a0ae8294d2e274ace1c5f7ab09353bae9da78a5050308828b4100f')
sha256sums_x86_64=('674f61f20ff306f3100cf9200e4c36c4b70278b5bef2884549819b942a89c863')

package() {
  install -Dm755 "${srcdir}/claude-${pkgver}-${CARCH}" "${pkgdir}/opt/claude-code/bin/claude"

  install -dm755 "${pkgdir}/usr/bin"
  cat > "${pkgdir}/usr/bin/claude" <<'WRAPPER'
#!/bin/sh
export DISABLE_UPDATES=1
export DISABLE_INSTALLATION_CHECKS=1
exec /opt/claude-code/bin/claude "$@"
WRAPPER
  chmod 755 "${pkgdir}/usr/bin/claude"

  install -Dm644 "${srcdir}/cc-legal" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
