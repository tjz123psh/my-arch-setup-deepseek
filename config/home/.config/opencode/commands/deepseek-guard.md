---
description: 查看 DeepSeek Guard 状态，或用 on/off 恢复、关闭当前会话保护。
---

执行 DeepSeek Guard 会话控制命令：

```text
$ARGUMENTS
```

日常只使用无参数、`on` 或 `off`。插件会在 `command.execute.before` 中验证并应用参数。只简短报告插件返回的状态，不要执行其他任务，也不要自行声称状态已经改变。
