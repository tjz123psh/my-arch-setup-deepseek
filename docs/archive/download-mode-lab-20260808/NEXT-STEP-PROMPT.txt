你现在接手 `/home/pang/Projects/my-arch-setup-deepseek`。先完整阅读：

- 根目录 `AGENTS.md`
- `download-mode-lab/CHECKPOINT.md`
- `download-mode-lab/FINAL.md`
- `download-mode-lab/03-mirror-plan.md`
- `download-mode-lab/README.md`
- `download-mode-lab/bin/mirror-plan.py`
- `download-mode-lab/tests/mirror-plan-test.py`
- 当前 `git status` 和所有既有 dirty diff

当前事实（不要重新解释成“已应用”）：

- 第一步默认移除外部 XferCommand 已完成；第二步同步时序整理仍在工作树 dirty diff，
  已有主项目回归通过。
- 第三步动态镜像 planner 仅在 `download-mode-lab/`，最终本地回归 41/41，完整 lab
  连续两轮通过；官方/archlinuxcn 真实只读探测已完成多轮，结果只在实验临时目录。
- 独立 reviewer 的最终 blocker-only 结论是“无阻断发现”；它没有在只读环境独立跑会写
  文件的完整套件，因此不要把 reviewer 的检查说成测试 PASS。
- 没有任何主代码 mirror-plan apply，没有修改宿主 pacman/mirrorlist，没有安装/删除包，
  没有服务/网络/内核/GRUB/VM 操作。

本轮唯一任务：只做 integration **dry-run 设计与最小 diff 展示**，不要直接修改文件：

1. 精读当前 `scripts/01-mirror.sh` 和 `scripts/03-packages.sh` 的仓库/镜像配置时序；
2. 设计只涉及 `scripts/01-mirror.sh`（必要时才说明为何必须同时改一个测试文件）的最小
   forward patch：在首次官方同步前运行有界、只读的 probe，调用 planner，读取
   `status/applyable/expires_at`；
3. 只允许 `status=ok && applyable=true` 生成临时 mirrorlist，并用权限保持不变的备份、
   临时文件、原子 rename；`degraded/unavailable/stale/future/invalid` 必须保留旧列表，
   且不能继续执行依赖新列表的同步；
4. 给出 fake-root/pacman mock 回归和失败注入：探测超时、Range 不支持、过期输入、空结果、
   不足 fallback、URL 非法、原子 rename 失败；失败查询不能伪装成空或健康；
5. 展示只读 dry-run diff、测试命令、备份路径格式、精确 rollback 命令；不要调用 sudo/gsudo，
   不运行主安装器，不写 `/etc`，不安装 downloader，不操作 KVM/VMware/物理机；
6. 不顺便重构 AUR、keyring、服务、Hyprland/DMS、桌面配置或下载后缓存。

完成后必须停下，等待用户明确批准 integration。没有批准时不得编辑
`scripts/01-mirror.sh`、`install.sh` 或任何宿主配置。
