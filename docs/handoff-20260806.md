# Handoff — 2026-08-06（会话交接，新对话接手依据）

本文件是 2026-08-06 会话的完整交接记录。配合 `AGENTS.md`（工作规则）、
`README.md`（使用）、`docs/project-vision.md`（产品决策）、
`docs/handoff-20260805.md`（8月5日历史）使用。**新对话先读本文件**。

## 1. 当前状态（最后确认，审查修复 + batch20 后更新）

- **仓库**：`/home/pang/Projects/my-arch-setup-deepseek`（main，公开 GitHub，可匿名 clone；2026-08-07 确认公开）
- **HEAD**：`68ebc1e` + 本轮审查修复待提交（工作区有改动，见第 11/12 节）
- **审查 + 修复完成**（2026-08-06）：精读全部代码/manifests/config 后修复 7 处（sudo 一次密码、dms 插件、桌面选择过滤、paru 重复构建等），batch20 全新重装验证通过（详见第 12 节）
- **batch18 验证完成**（2026-08-06）：最新代码 `install.sh -d niri -t vm` 全新重装闭环 10/10 通过，dms 自动启用，greetd→niri→dms 图形链路启动成功（详见第 10 节）
- **项目定位**：重装 Arch 后 `git clone → ./install.sh` 一键恢复 ASUS 工作站（Niri/Hyprland、包、AUR、配置、服务）。用户当前工作流是**反复重装验证**（用 virt-viewer + host HTTP 传输代码；strap 入口也可匿名 https clone，物理机无海外网络时走 U 盘离线路径）。

## 2. 安装器结构（当前代码）

```
strap.sh        root 入口（https clone 公开仓库可匿名，物理机无海外网络时失败，见第 6 节）
install.sh      主安装器：-d niri|hyprland|both|none；-t vm|physical；-y/--assume-yes；
                pre-flight 设 sudoers 99-install-nopasswd（timestamp_timeout=240 + 临时
                NOPASSWD，一次密码管全程含 makepkg 的 sudo -k；99-cleanup 删除恢复）
scripts/
  00-utils.sh   TARGET_USER/TARGET_HOME、run(sudo)、as_user、ensure_fzf、mark_done
  01-mirror.sh  aliyun/ustc/tuna 多镜像；CN_MIRROR 或 aliyun 探测；reflector 兜底
  02-system.sh  base-devel/git/python3/curl + Syu
  03-packages.sh 按清单安装；archlinuxcn 引导；rustup 预装（先清残留 rust/cargo/rustfmt）；
                 批量失败自动重试一次，仍失败逐个装并统计，失败>0 则 exit 1（不 mark_done）
  04-drivers.sh physical 才执行（AMD/NVIDIA/ASUS + supergfxd）
  05-niri.sh / 05-hyprland.sh
  06-aur.sh     14 recipe makepkg 构建；失败 exit 1（resume 重试）；单次 pacman -U
  07-config.sh  physical-v1 全量映射（233 unique，VM/physical 同套）；先备份
  08-services.sh 用户 units（as_user 带 XDG_RUNTIME_DIR）；greetd/dms-greeter→niri；
                 服务启用（vm/physical 一致，VM 跳过 libvirt net-autostart）；
                 用户组 input/docker/libvirt；GRUB 主题（cp -r + 删 GRUB_TERMINAL +
                 强制 gfxterm + guard）+ grub-mkconfig；paru.conf
  09-settings.sh locale(zh_CN)/时区/主机名/zram/标准目录 + dms 插件依赖
                 （~/.cache/checkallupdates、/.snapshots、/home/.snapshots、
                  shorin-screenrec-menu→/usr/local/bin）
  99-cleanup.sh pacman -Sc + .aur-build + 删 99-install-timeout（恢复 sudo 默认）
manifests/      workstation-packages.tsv(208) config-mappings.tsv(233 unique)
                aur-recipes.tsv(14)
config/         332 文件（含 config/home/.local/bin/vellum 全家桶 4 个二进制）
third_party/aur/ 14 recipe；tests/ 2 个数据一致性测试
```

## 3. 提交与推送状态（batch18 后：全部已推送）

```
68ebc1e docs(handoff): full session handoff for 2026-08-06 with complete review   ← 已 push
334affc fix(services): as_user 补 XDG_RUNTIME_DIR（dms.service 等用户 units 才能 enable）
b5eb639 fix(packages): 预装 rustup 前清残留 rust/cargo/rustfmt（resume 冲突）
4405209 fix(install): sudo timestamp_timeout=240（一次密码管全程）+ 06 AUR 失败 exit 1
0d748aa fix(packages): rustup 预装（解决 rust/cargo 批量冲突）+ 修 local 语法
992b646 fix(packages): 批量失败重试一次 + 失败统计不吞（不 mark_done）
fe20615 docs(handoff): batch17-vm 全新重装闭环验证记录
24928a8 fix(settings): 09 创建 dms 插件依赖 + 部署 shorin-screenrec-menu
```
2026-08-06 已 `git push origin main`（b119269..68ebc1e），gitleaks dir + git 历史扫描均无泄露。

## 4. 本轮全部修复（按问题归类）

| 问题 | 修复提交 |
|---|---|
| dms.service 重装后 disabled（桌面加载不出）| 334affc（as_user XDG_RUNTIME_DIR） |
| rustup 与 rust/cargo 冲突（含残留）| 0d748aa + b5eb639 |
| 镜像超时批量安装失败被吞 | 992b646（重试 + 失败不 mark_done） |
| AUR 构建失败被吞（不重试）| 4405209（06 exit 1） |
| 安装全程 sudo 反复要密码 | 4405209（sudoers timestamp_timeout=240，99 恢复） |
| dms 插件依赖缺失（插件启动失败）| 24928a8（09 建依赖 + shorin-menu 入库部署） |
| GRUB 主题静默不生效（gfxterm/GRUB_TERMINAL）| c93288e |
| GRUB 块缺 /etc/default/grub guard | a7ff48a |
| cp -a 在 vfat /boot 中止安装 | 80af58e（cp -r） |
| VM 模式 guest libvirt 网络与宿主冲突 | 80af58e（VM 跳过 net-autostart） |
| 安装完成 ASSUME_YES unbound | d13beca |
| README 映射计数过时 | 6d69a96 |

## 5. 验证记录

- **batch16-physical**（2026-08-06）：physical 模式最新代码验证通过（1119 包、驱动、GRUB 主题、服务）；发现 ASSUME_YES/gfxterm/fuzzel-ime 三问题（已修）
- **batch17-vm**（2026-08-06）：全新重装闭环验证通过（插件依赖自动就位、零手工修补）
- **用户 archlinux VM**：多次重装实战，暴露 03 冲突/06 失败/dms disabled 等问题（已逐一修代码）
- **注意**：334affc（as_user）之后**尚未做过完整重装验证**——新对话第一次重装验证需确认 dms 自动启用

## 6. 已知问题 / 待办（batch18 后更新）

1. ~~push 8 个提交~~ **已完成**（2026-08-06，gitleaks 扫描干净）
2. ~~完整重装验证（batch18）~~ **已完成**（见第 11 节）：最新代码 `install.sh -d niri -t vm` 全程通过、dms 自动启用、greetd→niri→dms 图形链路启动成功
3. **用户 archlinux VM 当前状态**：重装后 dms.service 未启用（用户在 tty2 可 `systemctl --user enable dms.service && start` 恢复）；若用户继续重装，最新代码已修复（batch18 已确认 dms 自动启用）
4. **strap.sh 匿名 https clone**：仓库已公开（2026-08-07 确认），可匿名 clone；物理机无海外网络时 clone 仍失败（网络问题非认证）——走 U 盘离线路径（见 physical-offline-install.md）
5. **脚本逻辑无自动化测试**（tests/ 只覆盖数据一致性）——bash -n 通过——简单优先可接受，如需加固再加
6. ~~【新发现】dms-shell provider 隐式选择~~ **已修**（本轮）：manifest `dms-shell` 拆分为 `dms-shell-niri`（wm-niri）/`dms-shell-hyprland`（wm-hyprland），03 按 DESKTOP_ENV 过滤 wm 模块——选 niri 不再冗余装 hyprland（batch20 验证 1017 包，hyprland 未装）
7. ~~【基础设施】验证 VM 域模板缺 video 设备~~ **已解决**：batch18 起模板带 `egl-headless + rendernode`（batch18.xml 可复用）
8. **【观察】makepkg 用 `sudo -k` 清 sudo 缓存**（makepkg 安全设计）：每步 pacman 调用都清除 timestamp，timestamp_timeout 方案对它无效——本轮改为 pre-flight 临时 NOPASSWD 解决（batch20 实测 sudo 提示 = 1 次）
9. **【观察】VM 时钟滞后 host**：guest 时钟在校正前可能慢 ~25 分钟，journalctl boot 划分与 symlink mtime 可能误导排查；判定服务是否自动启动以"安装完成后的首次重启"为准

## 7. 用户环境 / 操作流程（重要）

- **archlinux VM**：`virsh -c qemu:///system`（system libvirt），IP 192.168.122.216，pang/123
- **验证 VM（batch）**：`qemu:///session`（用户模式，无需改 /home 权限）；基线
  `~/.local/share/my-archlinux-setup/vm-validation-20260801/disks/baseline-handoff.qcow2`；
  SSH key `.../ssh/id_ed25519`；hostfwd 端口 22223；域模板参考
  `vm-validation-20260816/batch16-physical.xml`
- **代码传输**：host `cd 仓库 && tar --exclude='.git' -cf /tmp/repo.tar .`
  然后 `cd /tmp && python3 -m http.server 8000`；VM 内
  `curl -m 60 -s http://192.168.122.1:8000/repo.tar -o /tmp/repo.tar && tar -xf /tmp/repo.tar`
- **重装后远程访问**：sshd/serial console 默认不启用（符合 host 快照）——重装后只能
  virt-viewer 图形登录或离线 nbd 修（见 8 节）
- **dms 排障**：virt-viewer 里 Send Key → Ctrl+Alt+F2 切 tty2 诊断（systemctl --user / journalctl）

## 8. 关键技术笔记（踩过的坑）

- **runuser 不带会话环境**：`runuser -u user -- systemctl --user` 必须补
  `XDG_RUNTIME_DIR=/run/user/<uid>`（334affc）
- **rustup**：conflicts=rust/cargo/rustfmt 但 provides 三者；批量装时依赖 cargo 的包
  （cargo-audit）会拉 rust 包冲突——先装 rustup（0d748aa），resume 时先清残留（b5eb639）
- **grub-mkconfig 主题**：只在 gfxterm=1 时应用 GRUB_THEME；legacy GRUB_TERMINAL 会覆盖
  INPUT/OUTPUT（serial console 配置会静默丢主题）——删 GRUB_TERMINAL + 强制 gfxterm（c93288e）
- **cp -a 到 vfat**：chown 失败返回非零中止脚本——用 cp -r（80af58e）
- **sudo 一次密码**：/etc/sudoers.d/99-install-timeout（timestamp_timeout=240）——
  普通用户首 sudo 后写入，99-cleanup 删除
- **qcow2 崩溃遗留**：refcount 损坏 qemu 自动修复（首次启动慢属正常）；
  libvirt 网络规则损坏 `systemctl restart libvirtd` 重建

## 9. 产品决策提醒（勿偏离）

简单优先、面向个人；数据资产（config/manifests/recipes）是核心；host 快照即目标；
不改分区/不接管 grub-install/不复制凭据；greetd 登录；VM/physical 安装路径一致
（差异仅驱动 04 + libvirt 网络 autostart）。详见 project-vision.md。

## 10. batch18 完整重装验证（2026-08-06，最新代码闭环）

**目的**：确认 334affc（as_user XDG_RUNTIME_DIR）、4405209（sudo 一次密码）、
b5eb639（rust 清理）之后的**最新代码**全新重装闭环：全程通过、dms 自动启用、
重启后自动进图形界面。

**方法**：session libvirt；overlay 64G（backing `baseline-handoff.qcow2`）；
UEFI；hostfwd 22223；host HTTP 传代码（tar 51M → 10.0.2.2:8000）；
`./install.sh -d niri -t vm`（非交互，SSH 无 pty）。

**结果：10/10 步骤全部 PASS，INSTALL_EXIT=0**（01-mirror → 09-settings → 99-cleanup）。

核验明细（全部 PASS）：

| 项 | 结果 |
|---|---|
| 安装步骤 | 10/10，`.install_progress` 全标记 |
| dms 自动启用 | `systemctl --user is-enabled dms.service` = **enabled**；symlink 在 `~/.config/systemd/user/graphical-session.target.wants/dms.service` |
| 图形链路 | greetd active；进程链 `greetd → dms-greeter --command niri → niri -c → qs -p`（登录界面 niri + DMS 壳） |
| 服务 | greetd/bluetooth/docker/libvirtd 4 个 enabled |
| 用户组 | pang: libvirt docker input wheel |
| GRUB 主题 | `GRUB_THEME` + `GRUB_TERMINAL_OUTPUT=gfxterm` 已设；grub.cfg 6 处 Elegant（loadfont + 主题） |
| locale | LANG=zh_CN.UTF-8 |
| 插件依赖 | `~/.cache/checkallupdates`、`/.snapshots`、`/home/.snapshots`、`/usr/local/bin/shorin-screenrec-menu` 自动就位 |
| dms 插件文件 | `~/.config/DankMaterialShell/plugins/{ShorinScreenrec,dankmaintenance}/plugin.json` + settings.json 部署（首次运行自动生成配置目录） |
| AUR | 14/14（含 greetd-dms-greeter-git 1:0.0.0.r23） |
| 包数 | 1034 |
| sudoers | 安装后 `/etc/sudoers.d/` 仅剩 90-cloud-init-users，**99-install-timeout 被 99-cleanup 正确删除** |
| 收尾 | 非交互不重启（符合设计）；ACPI 关机干净；qemu-img check 无错误 |

**sudo 只输一次的验证说明**：验证基线 `baseline-handoff.qcow2` 的 pang 用户是
NOPASSWD sudo（自动化便利），无法直接观察密码提示次数；已验证替代证据为
4405209 的机制本身：pre-flight 写入 `99-install-timeout`（timestamp_timeout=240）
→ 99-cleanup 删除。代码审查确认首 sudo 后 240 分钟内不再提示。

**过程中修复的基础设施问题（非安装器 bug）**：batch16/17 域模板均无 `<video>`
设备，guest 无 `/dev/dri`，greetd 图形会话启动失败（`greeter exited without
creating a session` → start-limit-hit）。batch18 域补上
`<video type=virtio accel3d> + <graphics type=egl-headless> + <gl rendernode=
/dev/dri/by-path/pci-0000:35:00.0-render>` 后图形链路完整启动。注意 **勿用
spice graphics**（qemu 11 缺 gstreamer 视频编码器，启动报 invalid video codec）。

**新发现（见第 6 节待办 6/7）**：dms-shell provider 隐式选择导致冗余装 hyprland；
验证 VM 模板应默认带 egl-headless video。

**证据**：overlay `vm-validation-20260816/disks/batch18.qcow2`（11.6G，保留）；
域模板 `vm-validation-20260816/batch18.xml`（含 video 配置）；
host 安装日志 `/tmp/batch18-install-host.log`。

## 11. 全面审查与修复（2026-08-06，用户要求"完整安装零 bug + 减少 sudo + dms 搞好"）

精读全部代码（strap/install/00-09/99 + manifests + config + 插件 StartupCheck）
后修复 7 处：

| # | 问题 | 修复 |
|---|---|---|
| 1 | **sudo 次数**：makepkg 每次 pacman 调用都 `sudo -k`（清缓存），timestamp_timeout=240 对它无效，AUR 阶段每装一个缺依赖就提示一次密码（batch19 实测 7 次） | install.sh pre-flight 写 `99-install-nopasswd`（timestamp_timeout=240 + 临时 `TARGET_USER ALL=(ALL) NOPASSWD: ALL`）；`sudo -k` 对 NOPASSWD 无效 → 全程一次密码。99-cleanup 删除恢复。**batch20 实测 sudo 提示 = 1 次** |
| 2 | **dms-shell provider 隐式选择**：manifest `dms-shell` 触发 pacman provider 提示，非交互默认选 hyprland 版（冗余装 hyprland） | manifest 拆为 `dms-shell-niri`（wm-niri）/`dms-shell-hyprland`（wm-hyprland）；03 按 DESKTOP_ENV 过滤 wm 模块（niri 不装 hyprland 包，反之亦然；-d none 都不装） |
| 3 | **paru 重复构建**：06-aur 先 build paru 后循环里 continue 条件用未更新的 HAVE_PARU → paru 构建两次 | build 成功后 `HAVE_PARU=true` |
| 4 | **AUR 偶发失败**：makepkg -s 依赖解析瞬断（fuzzel-ime-git 的 fcft/tllist，batch16 也遇过） | 06-aur 失败 recipe 自动重试一次 |
| 5 | **录屏引擎**：shorin-screenrec-menu 默认 wl-screenrec（GPU），VM 无 GPU 启动即失败（handoff 8月5日记录的手工修复项） | 09-settings 按机型预置 `~/.cache/shorin-screenrec/engine`（vm→wf-recorder / physical→wl-screenrec） |
| 6 | **03 空数组边界**：未来过滤后 OFFICIAL 可能为空导致 `pacman -S` 无参数报错 | 空数组 guard |
| 7 | **README 数字过时**（156→173 官方 + 14 AUR） | 更新 |

**排查关键笔记**：
- **makepkg 的 sudo -k**：`/usr/bin/makepkg` 第 242 行 `cmd=(sudo -k "${cmd[@]}")`——makepkg 故意每次清缓存（安全设计），这是 AUR 多次 sudo 的真正根因（不是安装器 bug，但 NOPASSWD 方案绕过）
- **sudo 缓存 per-tty**：排查时新 SSH 连接是新 tty（无缓存），属正常；判定缓存生效要在同一 pty 会话内
- **验证 sudo 次数的方法**：python pty 脚本（`/tmp/run-install-pty.py`）驱动 ssh -tt + 检测 `[sudo] password for` 提示并喂密码 + 计数；VM 里删 cloud-init 的 NOPASSWD、改普通授权 `pang ALL=(ALL) ALL`、设已知密码后真实验证

## 12. batch20 完整重装验证（2026-08-06，最终代码）

**方法**：新 overlay（backing baseline）+ egl-headless video；VM 内真实 sudo 环境
（删 NOPASSWD、密码 Test1234!）；python pty 交互跑 `./install.sh -d niri -t vm`。

**结果：全部通过**：
- **sudo 提示 = 1 次**（pre-flight 唯一密码；makepkg 全程免密）✅ 用户核心诉求
- 10/10 步骤 PASS，INSTALL_EXIT=0，16 分钟
- **1017 包**（batch18/19 的 1034 少了 17 个 hyprland 相关包——桌面过滤生效）
- `pacman -Q hyprland dms-shell-hyprland` = 未装；`dms-shell-niri 1.5.3-1` 已装
- AUR 14/14；录屏引擎 `~/.cache/shorin-screenrec/engine` = wf-recorder
- dms/dsearch.service enabled（graphical-session.target.wants symlink）
- 7 服务 enabled（greetd/bluetooth/docker/libvirtd/NetworkManager/grub-btrfsd/systemd-timesyncd）
- GRUB 主题 6 处 Elegant；locale zh_CN；标准目录；插件 ShorinScreenrec+dankmaintenance
- **99-cleanup 后 sudoers.d 仅剩 90-cloud-init-users**（NOPASSWD + timeout 均被删除，sudo 恢复要密码）
- **安装完成后的首次重启**：greetd 自动启动 → dms-greeter → niri → qs（DMS 壳）完整链路 ✅
- 登录链路静态就绪：`niri.desktop Exec=niri-session` → `systemctl --user start niri.service` → graphical-session.target → dms.service（host 实机同链路已验证）

**验证基础设施改进**：域模板带 egl-headless video（batch18.xml → batch19/20.xml）；
验证 VM 的 sudo 真实化方法（删 NOPASSWD + 设密码 + pty 计数）已沉淀，见第 11 节。

**证据**：overlay `vm-validation-20260816/disks/batch20.qcow2`（11.9G，保留）；
pty 安装日志 `/tmp/batch20-install.log`（含 sudo 提示计数）。

## 13. physical 模式对齐验证 + 下载健壮性修复（2026-08-06，batch21）

**目的**：physical 与 vm 安装颗粒度对齐（除真实 GPU 硬件外），batch21 用
`./install.sh -d niri -t physical` 完整验证（含本轮所有修复）。

**过程中发现并修复 3 个下载健壮性问题**（vm 模式 batch20 未触发，physical
大下载 + 网络抖动暴露）：

| 问题 | 修复 |
|---|---|
| **pacman libcurl 无总超时**：镜像连接挂起时安装无限卡死（fuzzel 依赖下载卡住） | 01-mirror 设 XferCommand：`curl -L -f --connect-timeout 15 --max-time 300 --retry 3 --retry-delay 2 -C - -o %o %u`（须插在 [options] 段内，追加 EOF 会落进 [multilib] 被忽略；`-f` 防止错误页被存成 .db 导致 GPGME 校验失败） |
| **makepkg DLAGENTS curl 无超时**：源码下载（codeberg）挂死 | 06-aur 给 /etc/makepkg.conf 的 http/https DLAGENTS 加 `--connect-timeout 15 --max-time 600` |
| **fuzzel-ime-git 源码不可达**：codeberg `/archive/<commit>.tar.gz` 从 CN 网络连接挂起（host 与 VM 都验证），git smart-HTTP 可达 | recipe source 改为 `fuzzel::git+https://codeberg.org/dnkl/fuzzel.git#commit=<同一 commit>`（sha256 SKIP，srcdir 布局不变）；REVIEW.md 记录修改原因 |
| **03 rust 移除容错**：resume 时 db 残留（`pacman -Q` 误报已装但 `-Rdd` target not found）中止安装 | `pacman -Rdd` 失败 warn 继续 |

**batch21 结果（physical 模式，全新 overlay + 真实 sudo 环境）**：
- 10/10 步骤 PASS，INSTALL_EXIT=0（03 首次网络失败后 resume 续跑，06 之后全通）
- **包数 1106**（vs vm 1017，+89 驱动/硬件包）；hyprland/dms-shell-hyprland 未装
- 驱动包：nvidia-open-dkms/nvidia-utils/supergfxctl/asusctl/amd-ucode 全装，**supergfxd enabled + active**
- **libvirt default net autostart**（physical 特有）生效
- AUR 14/14（fuzzel-ime-git 经 git+ source 构建成功）
- 录屏引擎 `~/.cache/shorin-screenrec/engine` = **wl-screenrec**（physical）
- 7 服务 enabled；GRUB 主题 6 处；locale zh_CN
- **重启后**：greetd active → dms-greeter → niri → qs 完整链路；supergfxd/grub-btrfsd active
- 99-cleanup 后 sudoers.d 仅剩 90-cloud-init-users（sudo 恢复）

**结论**：physical 与 vm 安装路径完全对齐（差异仅真实 GPU 功能；代码路径、包策略、
桌面过滤、服务、录屏引擎、sudo 一次密码全部一致且验证通过）。

**证据**：overlay `vm-validation-20260816/disks/batch21.qcow2`（14.9G，保留）；
安装日志 `/tmp/batch21-install.log`（多轮 resume 记录）。

## 14. 收尾两项修复（2026-08-06，用户要求解决）

1. **下载静默（-sS）**：XferCommand（01-mirror）与 makepkg DLAGENTS（06-aur）的
   curl 加 `-sS`——成功时不再刷每条文件的进度条（pacman -Syy 从满屏变成 5 行
   `core/extra/multilib/archlinuxcn downloading...`），失败仍显示
   `curl: (22) 404` 等错误（换镜像 failover 逻辑不变）。VM 实测通过。
2. **strap.sh 匿名 clone 诊断**：默认 REPO_URL 是公开仓库（2026-08-07 确认公开），
   可匿名 https clone；clone 失败通常是无海外网络（物理机 CN 网络）。strap.sh 现在
   clone/pull 失败时输出 git 错误 + 三种出路：① 检查网络/代理 ②
   `REPO_URL=<可访问镜像>` ③ 不走 strap，U 盘离线传代码跑 install.sh。
   不再静默失败或误判。

## 15. 离线 AUR 源码包方案（2026-08-06，物理机无海外网络）

**背景**：用户物理机（ASUS 工作站）直连 GitHub/codeberg 打不开（手机实测），
VM 有 VPN 才顺畅。物理机重装后 AUR 海外源码下载会失败（不是慢是连不上）。
方案：在能联网的机器上预下载全部 AUR 源码，物理机离线构建。

**交付物**（~/Downloads/）：
- `my-arch-setup.tar`（48M，仓库，含 06-aur SRCDEST 逻辑 + paru PKGBUILD 改动）
- `aur-sources.tar.gz`（1022M，SRCDEST 布局：git 裸镜像 + release 文件 + go-mod + cargo）

**改动**：
1. **06-aur.sh**：`.aur-sources/` 存在时设 `SRCDEST` + `GOMODCACHE` + `CARGO_HOME`
   （makepkg 源码缓存 + Go module 缓存 + cargo registry 缓存）
2. **paru PKGBUILD**：prepare() 的 `cargo update`/`cargo fetch` 加 `--offline`
   （离线时用本地 registry；build() 原本就是 `--frozen`）
3. **fetch-aur-sources.sh**（新，仓库顶层）：一条命令生成完整离线缓存
   （22 个 PKGBUILD source + go mod download + cargo fetch）

**离线验证（batch18，删默认路由断网）**：
- 13 个 AUR 全用缓存构建安装成功（含 greetd-dms-greeter 的 Go 依赖，
  dank-greeter/dank-qml-common 源码 "Skipped"=用缓存）
- paru 单独离线构建成功（cargo 缓存 + --offline）
- **14 个 AUR 全部可在无外网环境构建** ✅

**注意**：paru 首次手动测试报 "Disk quota exceeded"——验证 VM 的 /tmp 是
tmpfs（2G）限制；06-aur 构建目录在 home（磁盘），不受影响。

**物理机用法**：重装后 U 盘拷入两个 tar → 解包代码 + 解包 aur-sources 到
仓库 `.aur-sources/` → `./install.sh`（镜像走国内源，AUR 全离线）。

## 16. paru 离线构建修复（2026-08-07，用户 VM 实测失败）

**现象**：用户 VMware + KVM 都报 paru 构建失败：
`no matching package named 'alpm' found (crates.io index)`，prepare() 的
`cargo update --offline alpm alpm-utils` 失败。

**根因**：`cargo update` 需要 crates.io index（即使 --offline 也依赖本地 index
缓存完整）；且用户大概率拷到了**旧的 955MB aur-sources 包**（不含 cargo 缓存）。
（host 上用含 cargo 的 1022M 包复现 offline `cargo update` 实际是成功的——
所以缓存本身没问题，问题在"依赖 cargo update 联网/index" + 包版本。）

**修复（不依赖 cargo update）**：
- paru recipe 新增提交的 `Cargo.lock`（alpm 4.0.4 / alpm-sys 4.0.5，
  libalpm16 兼容），PKGBUILD source 加 `Cargo.lock` + 对应 sha256
- prepare() 改为：`cp -f ${srcdir}/Cargo.lock Cargo.lock` +
  `cargo fetch --locked --offline`（build() 本来就是 `--frozen`）
- 完全离线，无需 crates.io index
- fetch-aur-sources.sh 注释说明 Cargo.lock 同步维护

**验证**：host 用新 recipe + 缓存 + SRCDEST/CARGO_HOME 跑 makepkg，
paru 2.1.0-5 构建成功（无网络依赖）。

**用户需重拷**：新 `my-arch-setup.tar`（48M，含 Cargo.lock 修复）+
`aur-sources.tar.gz`（必须 1022M 版本，含 cargo 缓存；旧 955M 版不含）。

## 17. 全面差异审查修复（2026-08-07，host vs 仓库对齐）

用户要求"重新审查本系统配置/包，找安装器没考虑到的"，审查结果与修复：

**包差异**（host 186 显式 vs manifest）：真漏 3 个补入 manifest：
- sshpass / tcpdump（cli-tools）、virt-viewer（virtualization）
（其余差异：verify 前置/14 AUR/旧包名，非漏）

**配置差异**（host ~/.config 46 顶层 vs 映射 30）：
- ✅ 捕获：**MiSans 字体**（39M，5 ttf → config/home/.local/share/fonts/MiSans/）
- ✅ 捕获：**vellum 二进制**（ELF 2.5M → config/home/.local/bin/vellum，unit 的 ExecStart 依赖）
- ❌ 不捕获：**opencode**（275M 含 270M node_modules + opencode.json 含 API key/token/secret——
  凭据不能入库）；gh（认证凭据）；clash（配置私有）；chrome/QQ/Typora/obsidian 等（应用数据）
- 运行时（dconf/pulse/ibus/go/JetBrains）不捕获

**/etc 差异**：
- ✅ 补：**/etc/shells 加 fish + 用户登录 shell chsh 到 /usr/bin/fish**（09-settings，host 是 fish）
- snapper：**09 初始化 root/home 配置**（create-config；.snapshots 需 btrfs 子卷，先 rmdir 空的普通目录）
  - VM 实测：**root 成功**（snapper list 正常）；**home 在 VM 失败**（VM 基线 /home 非 btrfs 子卷，
    warn 容错）；**物理机 @home 子卷 → 会成功**（对齐 host）
- pam.d/dankshell 是 dms-greeter 登录 sync 自动生成（无需安装器）

**用户需自行备份的**（安装器不接管）：~/scripts 全套、~/.config/opencode（含凭据）、
~/.local/share/fonts 之外的用户数据（chrome/QQ/Typora/obsidian 等应用数据）。

**交付**：my-arch-setup.tar 重新打包（75M，含 MiSans/vellum/映射/09 改动）。
（后续更新：2026-08-07 16:43 最新包 78M，含 btrfs-scrub timer + snapper-timeline timer + ACL 对齐等修复，以 ~/Downloads/my-arch-setup.tar 当前文件为准。）

## 18. dms 插件回归修复（2026-08-07，VM 全新重装后 dankmaintenance 启动失败）

**现象**（用户 VM 重测新包，截图 vellum-2026-08-07_11-44-47）：dms 顶栏弹窗
`Maintenance Status 启动失败`，依赖检查未通过：需要 collect-status 可执行、
`~/.cache/checkallupdates` 存在、`/.snapshots` 与 `/home/.snapshots` 可读。

**根因**（本次回归，非 8/5 已知问题）：StartupCheck.qml 检查的 4 项依赖中，
**`/home/.snapshots` 在 09-settings 的 snapper 块里被删后没有恢复**：
1. 09 先建插件依赖目录 `run mkdir -p /.snapshots /home/.snapshots`
2. snapper 块 `run rmdir /home/.snapshots` 删掉空目录 → `snapper -c home
   create-config /home`
3. **VM 的 /home 不是 btrfs 子卷 → create-config 失败 → 只 warn 不重建目录**
   → `/home/.snapshots` 永久消失 → 插件启动检查 `test -r /home/.snapshots` 失败
（物理机 /home 是 @home 子卷，create-config 成功，不触发；8/5 VM 是手动补的目录，
走代码路径的全新重装才暴露）

**修复**（09-settings.sh snapper 块）：create-config 失败时恢复普通目录
`run mkdir -p /.snapshots` / `run mkdir -p /home/.snapshots`，保证插件依赖
始终存在（snapper 不可用时插件仍能加载，快照 chip 显示 "—" 即可）。

**验证**：`bash -n` 通过；根因链条与 StartupCheck.qml 检查项逐条对上。
VM 即时恢复方法（无需重装）：`sudo mkdir -p /home/.snapshots` 后重载 dms。

---

## 19. 2026-08-07 13:30 二次回归：snapper 权限/ACL 未对齐 + vellum 全家桶缺失（用户重装后插件仍失败）

**现象**（用户重装 11:51 包后仍报错，9 张截图 vellum-13:25）：dms 插件
`Maintenance Status 启动失败`依旧；另发现 `vellum-tray.service` failed、
Git 改动阅读 6 项 delta 配置缺失、btrfs-scrub@-.timer 未启用、snapper
root/home "配置不存在或无法读取" / "No permissions"。

**根因（§18 修复不完整，方向错误）**：
1. **snapper create-config 成功后 .snapshots 权限 750 root:root**——主机快照
   是 `drwxr-x--- root:root` + `group:wheel:r-x` ACL + `ALLOW_GROUPS=wheel`，
   普通用户可读；VM 上 create-config 生成的子卷无 ACL、config 无
   ALLOW_GROUPS → `test -r /.snapshots` 失败 → 插件仍启动失败。
   §18 只处理了 create-config **失败**分支，成功分支（VM root 成功）没对齐 ACL。
2. **vellum 全家桶只抓了 vellum**：主机 4 个二进制
   （vellum/vellum-tray/vellumctl/vellum-ui），config 只有 vellum →
   `vellum-tray.service` ExecStart 指向不存在的二进制 → failed；
   hypr/niri keybinds 引用 `vellumctl region/long/pin-last` 也失效。
3. **主机 ~/.gitconfig（delta 配置）从未纳入映射** → Git 改动阅读 6 项
   （core.pager/interactive.diffFilter/delta.navigate/side-by-side/
   merge.conflictstyle/diff.colorMoved）全未配置。
4. **btrfs-scrub@-.timer 主机 enabled，08-services 从未启用**。
5. **quicksave 权限正则漏 `No permissions`（带空格）** → snapper 权限错误被
   误判为"错误并非权限不足，已拒绝修改授权"。

**修复**（均已验证）：
- 09-settings.sh：create-config 后对 root/home 执行
  `snapper set-config ALLOW_GROUPS=wheel SYNC_ACL=yes` + `setfacl -m g:wheel:r-x`
  `/.snapshots /home/.snapshots`（对齐主机 ACL；chmod o+rx 兜底）。
- config + 映射：新增 vellum-tray/vellumctl/vellum-ui（755）、
  `.gitconfig`（644，含 delta 全套配置）。映射 229 → 233。
- 08-services.sh：btrfs 根上启用 `btrfs-scrub@-.timer`。
- quicksave：正则加 `no ?permission` 变体。

**验证**：`bash -n` 通过；reconciliation 测试通过
（install=190 verify=18 total=208, mappings=233, recipes=14）。
